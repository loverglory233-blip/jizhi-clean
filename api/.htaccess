# 🛡️ 保护 API 目录下的敏感密钥与私钥文件，严禁外部 HTTP 直接下载
<FilesMatch "\.(pem|key|json|sql|env|log)$">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Order deny,allow
        Deny from all
    </IfModule>
</FilesMatch>

# 仅允许合法 PHP 请求执行
Options -Indexes
